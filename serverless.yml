service: aws-node-serverless-project

frameworkVersion: '3'

provider:
  name: aws
  region: ap-northeast-1
  runtime: nodejs16.x

plugins:
  - serverless-webpack
  - serverless-offline

custom:
  webpack:
    includeModules: true
    packager: 'yarn'

functions:
  hello:
    name: hello-lambda
    handler: ./src/lambda.handler
    timeout: 5
